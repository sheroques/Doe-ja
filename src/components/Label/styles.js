import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  label: {
    fontSize: 14,
    fontWeight: '600',
    color: '#3677C1',
    marginBottom: 4,
    textAlign: 'left',
    alignSelf: 'flex-start',
  },
});
